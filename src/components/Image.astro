---
// Import necessary scripts or styles here if needed
const { src, alt, width, height, Class } = Astro.props;
---

<img class={Class} src={src} alt={alt} style={{ width: width ? `${width}px` : 'auto', height: height ? `${height}px` : 'auto' }} />